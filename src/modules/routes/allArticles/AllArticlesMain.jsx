export default function AllArticlesMain({ children }) {
    return (
        <main className='all-articles-main'>
            <div className='all-articles-cont'>{children}</div>
        </main>
    );
}
